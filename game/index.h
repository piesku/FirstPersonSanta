#pragma once

enum texture_index {
	TEX_CHECKER,
	TEXTURES_LENGTH,
};

enum material_index {
	MAT_COLORED_UNLIT,
	MAT_TEXTURED_UNLIT,
	MATERIALS_LENGTH,
};

enum mesh_index {
	MESH_CUBE,
	MESHES_LENGTH,
};
