#pragma once

enum render_target_index {
	RENDER_TARGET_NONE,
	RENDER_TARGET_RENDER,
	RENDER_TARGET_MINIMAP,
	RENDER_TARGETS_LENGTH,
};

enum texture_index {
	TEX_NONE,
	TEX_RENDER_RGBA,
	TEX_RENDER_DEPTH,
	TEX_MINIMAP_RGBA,
	TEX_MINIMAP_DEPTH,
	TEX_CHECKER,
	TEXTURES_LENGTH,
};

enum material_index {
	MAT_COLORED_UNLIT,
	MAT_TEXTURED_UNLIT,
	MAT_POSTPROCESS,
	MATERIALS_LENGTH,
};

enum mesh_index {
	MESH_CUBE,
	MESH_QUAD,
	MESHES_LENGTH,
};
